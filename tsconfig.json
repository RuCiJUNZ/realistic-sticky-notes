{
  "compilerOptions": {
    /* 1. 基础输出设置 */
    "baseUrl": ".",
    "module": "ESNext",
    "target": "ES2020", /* 🟢 建议升级到 ES2020，Obsidian 的环境完全支持，代码更简洁 */
    "allowJs": true,
    "sourceMap": true,
    "inlineSources": true,
    /* 2. 模块解析 */
    "moduleResolution": "node",
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "skipLibCheck": true,
    "resolveJsonModule": true, /* 🟢 建议开启：允许导入 json 文件 */
    /* 3. React 支持 */
    "jsx": "react-jsx", /* 🟢 强烈建议：React 17+ 使用此选项，不用在每个文件里 import React */
    /* 4. 类型检查严格度 */
    "noImplicitAny": true,
    "strictNullChecks": true,
    "importHelpers": true,
    /* 5. 核心库定义 */
    "lib": [
      "DOM",
      "ESNext", /* 🟢 用 ESNext 直接包含所有新语法，不用写一长串 ES2015-2020 */
      "DOM.Iterable"
    ]
    /* 🔴 关键修改：彻底删除 "types" 字段 */
    /* 不要在这里列出 ["node", "lodash", "react"]！ */
    /* 删除后，TypeScript 会自动扫描 node_modules/@types 下的所有包 */
  },
  "include": [
    "**/*.ts",
    "**/*.tsx"
  ],
  "exclude": [
    "node_modules",
    "test",
    "coverage",
    "dist"
  ]
}